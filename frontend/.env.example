# Deixe vazio/comentado - o app usa automaticamente o mesmo endereco que carregou a pagina.
# So' preencha se quiser forcar outro backend.
# VITE_API_URL=http://localhost:8080
# VITE_WS_URL=ws://localhost:8080/ws
