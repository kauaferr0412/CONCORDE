# Segredos - NUNCA versionar (a chave do GCS vai como variavel de ambiente GCS_CREDENTIALS_JSON,
# nao como arquivo, mas por seguranca qualquer .json de credencial que aparecer aqui e' ignorado)
*service-account*.json
*credentials*.json

# Backend
backend/target/
backend/data/
backend/.mvn/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.env

# Deploy (producao)
.env.prod

# IDE / SO
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db
